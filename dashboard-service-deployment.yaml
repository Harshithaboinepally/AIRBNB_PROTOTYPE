apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: dashboard-service
  name: dashboard-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: dashboard-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: dashboard-service
    spec:
      containers:
        - env:
            - name: DB_HOST
              value: mongodb
            - name: DB_NAME
              value: airbnb_db
            - name: DB_PORT
              value: "27017"
            - name: FRONTEND_URL
              value: http://localhost:3000
            - name: NODE_ENV
              value: development
            - name: PORT
              value: "5006"
            - name: SESSION_SECRET
              value: dashboard-service-secret
          image: airbnb_prototype-dashboard-service
          imagePullPolicy: IfNotPresent
          name: dashboard-service-container
          ports:
            - containerPort: 5006
              protocol: TCP
      restartPolicy: Always
      volumes:
        - configMap:
            name: dashboard-service-cm0
          name: dashboard-service-cm0
        - name: dashboard-service-claim1
          persistentVolumeClaim:
            claimName: dashboard-service-claim1
